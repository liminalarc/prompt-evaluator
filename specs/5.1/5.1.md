---
id: 5.1
title: Adopt LitmusAI across Cortex Golf & Stormboard (discover → onboard → improve every prompt)
links: [3.1, 2.2, 1.2, 4.4, 1.16, 1.17, 1.18]
deferrals:
  - what: "6 Golf vision/multimodal prompts (auto-map-centerline-detection, course-layout-analysis, overview-map-registration, per-hole-polygon-detection, scorecard-extraction, routing-review)"
    why: "Fixture.Input is text-only (no image fixtures); can't be walked end-to-end with what exists"
    to: 1.17
  - what: "3 Golf tool-loop prompts (facility-enrichment, routing-hole-search, scorecard-search)"
    why: "harness has no tool/web_search execution; one-shot run is low-fidelity"
    to: 1.18
---

## Problem
LitmusAI is built and running on dev, but has never been pointed at real prompts from a
real app. We need to prove the whole loop end-to-end on production prompts from two sibling
apps — Cortex Golf and Stormboard — done **by hand** so the workflow is understood, not
automated away. Payoff is threefold: (a) learn the tool by using it, (b) measurably improve
those two apps' prompts, (c) leave a realistic, populated test-bed for future features.

## Value
As the LitmusAI maintainer I want a repeatable, hands-on workflow for onboarding an external
app's prompts and improving them, so that I learn the tool, ship better prompts back to
Cortex Golf and Stormboard, and have real data to build future features against.

## Scope
**In:** Discover + inventory every prompt in both apps (**54** — T1 reconciled: Golf 15,
Stormboard 39 = 37 `.md` + 2 inline C# system prompts; the sizing scan's 52 was `.md`-only).
Stand up each app as an org in LitmusAI (dev). Walk **every** prompt through
the per-prompt playbook below (register → dataset → baseline → improve → re-eval → backport →
log). All steps performed manually.
**Out:** Zatomic-backed registry — manual copy-in stays (→ 3.1). Automated prompt sync from
the source repos (none; copy by hand). Advisory automation (→ 2.2). New eval/review features —
use what exists; findings feed the backlog, not this spec.

## Source locations & inventory
T1 reconciled 2026-07-18 — **full per-prompt catalog** (purpose/model/I·O shape/call site/capture
feasibility) in [catalog.md](catalog.md); tracking checklists in [T3](5.1.T3.md) (Golf) / [T4](5.1.T4.md) (Stormboard).
- **Cortex Golf** (15) — repo `C:\Development\code\Golfstat\golfstat`, `…/AiService.WebApi/Prompts/*.md`.
- **Stormboard** (39) — repo `C:\Development\stormboard`; 37 `.md` under `StormBoard.Claude/Prompts/` **+ 2
  inline C# system prompts** (`wizard-prompts` `ClaudeWizardPromptEngine.cs:23`, `asset-mapping`
  `StormBoard.WebApi/…/CodebaseEndpoints.cs:1200`) — a smell (bypass `FilePromptStore`); extract-to-`.md`
  folds into their backport (T4).

## The per-prompt playbook (the repeatable steps)
Run these for each prompt, by hand:
1. **Study** — read the prompt in its home repo; note purpose, model, input/output shape,
   call site, and what upstream feeds it.
2. **Register** — copy the text verbatim into the app's org as Prompt + Version `v1` (baseline).
3. **Dataset** — assemble fixtures: capture real inputs (± outputs) from the app as the
   ground-truth corpus; fill coverage gaps with synthetic fixtures seeded from captures (1.2).
4. **Scorers** — pick scorer(s) fit for the output: deterministic / LLM-judge / human.
5. **Baseline** — run `v1 × dataset × scorers`; record the baseline score.
6. **Diagnose** — read the low scores / failures; form one hypothesis to improve.
7. **Improve** — author `v2`; run `v2 × same dataset × same scorers`.
8. **Compare** — check movement vs prior version (regression detection); iterate `v3+` until
   satisfied or diminishing returns.
9. **Backport** — copy the winning version back into the source app (or decline, with reason).
10. **Log** — one line of learnings per prompt: what moved the needle.

## Tasks
- **T1** — Discover & inventory prompts (both apps) → a catalog.
- **T2** — Workspace setup + validate the playbook on the first prompt.
- **T3** — Cortex Golf: walk all 15 prompts.
- **T4** — Stormboard: walk all 37 prompts.

## Acceptance criteria
- [x] Prompt inventory complete (Golf 15, Stormboard 39) — catalogued w/ purpose, model, input shape, call site (T1: [catalog.md](catalog.md), 54 total).
- [x] Per-prompt playbook documented + validated on ≥1 prompt (daily-briefing, T2).
- [x] Both apps exist as orgs in LitmusAI dev.
- [ ] Every **walkable** Cortex Golf prompt walked end-to-end (register, dataset, baseline, ≥1 improvement, score movement) — 6 walkable (**2/6 done**: daily-briefing, round-debrief); **9 blocked re-homed: 6 vision→[1.17](../1.17.md), 3 tool→[1.18](../1.18.md)**.
- [ ] Every **walkable** Stormboard prompt walked end-to-end, same bar — 39 (near-all text; classify at T4 start).
- [ ] Each improvement backported to source, or declined with a recorded reason (LitmusAI signals only — [1.16](../1.16.md)).
- [ ] Per-prompt learnings logged.

## Plan (thin slices)
1. [x] T1 discovery/inventory -> (catalog.md; 54 reconciled)
2. [ ] T2 setup + playbook proof -> <sha>
3. [ ] T3 Cortex Golf (15) -> <sha>
4. [ ] T4 Stormboard (39) -> <sha>

## Decisions
- 2026-07-18 — Exhaustive DoD chosen by user: every prompt walked end-to-end; DONE is intentionally distant.
- 2026-07-18 — Work is manual by design (learning exercise); no import automation in scope. User does
  every LitmusAI op by hand (orgs included); the maintainer's role is the catalog + walk-along guidance.
- 2026-07-18 — One combined spec, both apps; playbook lives here, referenced by T3/T4.
- 2026-07-18 (T1) — Inventory reconciled to **54** (was 52): a deep sweep found 2 Stormboard inline C#
  system prompts (`wizard-prompts`, `asset-mapping`) the `.md`-only scan missed (rationale in [catalog.md](catalog.md)).
- 2026-07-18 — DoD re-scoped to **walkable** prompts (user): 9 Golf prompts can't be walked with what
  exists — 6 vision (no image fixtures)→[1.17](../1.17.md), 3 tool-loop (no tool eval)→[1.18](../1.18.md);
  re-homed, not degraded. Findings F1/F2/F3 → specs [1.16](../1.16.md)/[1.17](../1.17.md)/[1.18](../1.18.md).

## Verification / evidence
Per-prompt runs visible in LitmusAI dev (Prompt × Version × Dataset × Scorer with score
movement); T3/T4 checklists ticked; backport commits in the source repos.

## Progress log
- 2026-07-18 — spec set IN PROGRESS; T1 complete: [catalog.md](catalog.md) — all 54 prompts catalogued
  (purpose/model/input+output shape/call site/capture feasibility), completeness verified by redundant
  per-repo deep sweeps (Golf 15 complete; Stormboard 37 `.md` + 2 inline found). Inventory 52→54.
- 2026-07-18 — T2 shakeout **complete**: user created both orgs by hand; daily-briefing walked the full
  playbook (v1 0.55 → v2 0.88, +0.326 in Analytics). Surfaced 7 bugs + a UX round → all homed + shipped in
  **2.8**. daily-briefing pinned pending 2.8's eval-loop UI.
- 2026-07-18 — T3 resume (post-2.8, dev v0.14.0): daily-briefing **backported** to Golf (`660a3ff2`),
  T3 1/15. Runbook refreshed to live 2.8 UI. F1/F2/F3 → specs 1.16/1.17/1.18; 9 blocked Golf prompts
  re-homed; DoD re-scoped to walkable.
- 2026-07-18/19 — **round-debrief** walked (T3 **2/6 walkable**): v2 wins on the data-conditional rubric
  (avg 0.79→0.88; sparse fixture 0.60→0.90), **backported** to Golf `9ba2ad3c`. Surfaced findings B8/R1–R5
  → all homed in **[2.12]**; B8/R2/R5 **shipped** 2026-07-19 (the quick-fix slices), R1/R3/R4 remain. Fill
  sheet condensed to its durable record. ⚠️ v4 real-model (Sonnet 4.6) validation still owed — see the fill.
