---
id: 5.1.T2
title: Workspace setup + validate the playbook on the first prompt
---

## Goal
Stand up both apps as orgs in LitmusAI dev, then run the very first prompt through the full
per-prompt playbook (orchestrator, steps 1–10) end-to-end. The first run is the shakeout —
refine the playbook wording from whatever it surfaces before scaling to the other 51.

## Done when
- [x] `Cortex Golf` and `Stormboard` orgs exist in LitmusAI dev. → created manually by user 2026-07-18.
- [x] One prompt walked the full playbook: registered v1, dataset built, baseline scored, ≥1 improvement iteration, score movement observed. → daily-briefing v1 0.55 → v2 0.88 (Analytics compare confirms +0.326). See [fills/daily-briefing.md](fills/daily-briefing.md).
- [x] Playbook in `5.1.md` updated with any corrections the first run exposed. → 10-step playbook held; UI-level refinements captured in [runbook.md](runbook.md), 7 bugs + UX findings in [findings.md](findings.md) (→ 2.8).

## Notes
- Registration is manual copy-in (paste into the LitmusAI UI) — Zatomic-backed registry is future (3.1).
- Suggest picking a small, well-understood prompt for the shakeout (e.g. Golf `daily-briefing` or Stormboard `executive-summary`).
