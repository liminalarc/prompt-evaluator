---
id: 2.9a
title: Composite scoring — advanced (must-pass/veto gating + weight auto-tuning + subject-model-constant eligibility)
links: [2.9, 2.1, 2.15, 1.16, 5.1]
---

## Problem
The weighted composite ([[2.9]]) is a **mean** — every scorer contributes proportionally to its
weight and nothing can hard-fail a version. Two capabilities were deliberately left out of 2.9 and
have no home until now:

- **Must-pass / veto gating** — a scorer that, on failure, **zeroes** the composite (or bars a
  version from being backport-eligible) regardless of the other scorers. A weighted mean can let a
  strong judge score paper over a hard safety/format failure; a veto scorer prevents that. 2.9
  ships a mean, not a gate, on purpose — "revisit once weighted means are in use."
- **Weight auto-tuning** — learn the per-scorer weights from human labels ([[2.1]] review queue)
  instead of hand-setting them, so the composite tracks what reviewers actually value.
- **Subject-model-constant backport eligibility (R9, from 5.1)** — [[2.9]]/[[1.16]]'s eligibility +
  single-target derivation (`VersionStatusHandler`) keys series off (dataset × scorer identity) but
  **not** the subject model, so a version scored on a **stronger model** can win the "ship this" badge
  on the model, not the prompt. 2.9 closed the *scorer-config* confound; the *subject-model* confound
  (R5) was never closed for the backport engine. Surfaced live: round-debrief Current=v1 (Sonnet 4.6)
  badged `Backport target = v2` (Sonnet 5) over the honest real-model winner v7 (Sonnet 4.6).

## Value
As a prompt owner I want a scorer I can mark **must-pass** (so a hard failure can't be averaged away)
and weights that **learn from my review labels**, so the one "overall quality" number and the
backport signal reflect real quality bars, not just a hand-tuned average.

## Scope
**In (when picked up):**
- A per-dataset-scorer **must-pass/veto** flag on `ScorerConfig`; its effect on the composite
  (zero vs exclude-then-bar) and on [[2.9]]'s eligibility/target derivation is a design decision at
  plan-time.
- **Auto-tuned weights** derived from human-review labels ([[2.1]]), with the hand-set weight as the
  prior/override.
- **Subject-model-constant eligibility (R9)** — hold the subject model constant in [[2.9]]/[[1.16]]
  eligibility + target selection: only compare versions sharing `Current`'s subject model (exclude or
  flag cross-model series), the sibling of the existing same-scorer-config rule. Plus a **cross-model
  warning on the Deployment card**. Interaction with veto/weights is a plan-time design call.

**Out:** anything already shipped in [[2.9]] (per-scorer weights, the weighted-mean composite, the
composite trend + change table, the weighted backport signal).

## Acceptance criteria
- [ ] A scorer can be marked must-pass; a must-pass failure gates the composite / eligibility per the
      chosen semantics — proven by tests
- [ ] Weights can be auto-tuned from human labels, with a hand-set override — proven by tests
- [ ] Backport eligibility/target holds the subject model constant — a version scored on a different
      (stronger) model than `Current` can't win the target on model alone; cross-model series are
      excluded or flagged, and the Deployment card warns on a cross-model comparison (R9) — proven by tests

## Decisions
- 2026-07-20 — Split out of [[2.9]] at close-out (user decision): both were 2.9's explicit **Out**
  items with no spec home. Combined into one "advanced composite" spec rather than two, since both
  extend the same composite machinery. Origin: 2.9 §Out + the 1.16→2.9 backport-signal work.
- 2026-07-20 — **R9 folded in** (user decision, from [[5.1]] dogfooding): the backport recommender blended
  across subject models — round-debrief's Deployment card badged v2 (Sonnet 5) over the real-model winner
  v7 (Sonnet 4.6). Sibling of the same-scorer-config rule; belongs with the composite-scoring rigor here
  rather than a standalone spec. Origin: [5.1 findings R9](5.1/findings.md).

## Progress log
- 2026-07-20 — created (NOT STARTED) as the home for 2.9's deferred veto-gating + weight-auto-tuning.
- 2026-07-20 — scope extended with R9 (subject-model-constant backport eligibility) folded in from 5.1.
