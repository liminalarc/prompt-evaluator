---
id: 2.4
title: UX Overhaul — App Shell, Navigation & Design-System Foundation
links: [1.4, 1.7, 1.9, 2.1, 2.3]
---

## Problem
The app grew slice-by-slice and now reads as disconnected. Concrete failures found in a
full sweep of `web/`:

- **Navigation is flat and incomplete.** Topbar is `Home · Prompts · Datasets · Analytics`.
  `Home` is the walking-skeleton echo toy (`/api/echo`) — a dev artifact is the landing page.
  Eval runs have no nav entry (only reachable by drilling into a dataset; the run page's
  back-link points at `/datasets`, not its own dataset/prompt).
- **Organization** — the top-level boundary (1.9) — exists only as a dropdown inside `/prompts`;
  Datasets and Analytics ignore it, so the mental model breaks off the Prompts page.
- **The same Prompt×Dataset relationship is expressed three ways** — embedded in the prompt
  workspace (1.7), as a top-level `/datasets` list, and again behind `/analytics`'s own
  pickers — forcing re-selection and page-bouncing.
- **Every page is a long scroll of stacked bare forms** with no chrome: no shared header,
  no breadcrumb standard, no loading states (pages render blank until data arrives), and a
  generic "is the stack running?" box for every error.
- **The rich brand vocabulary is unused** — `.sb-badge`, `.sb-chip`, `.sb-card`, button
  variants/sizes, `.sb-field--error` all ship in `brand-tokens` but status is raw emoji and
  plain text, which is why it reads flat.
- **The core eval loop is invisible** — nothing narrates *author → dataset → scorers → run →
  scores → regressions → improve*, and there's no "how are my prompts doing?" view.

"Shipped" means the app is coherent and consistently navigable, reflects the brand, and
narrates the eval loop — **with no API or domain change**.

## Value
As an operator (prompt engineer) I want a coherent, consistently-navigable app that narrates
the eval loop and uses our brand system, so that I can move from prompt → dataset → run →
scores → regressions without re-orienting on every page.

## Scope
**In:**
- **App shell & navigation.** Redesigned topbar; a **persistent global organization context**
  (small signals-based org-context store) that scopes prompts, datasets, analytics, and the
  dashboard consistently; org switcher moves to the topbar. Consistent breadcrumb/back across
  detail pages. Runs made discoverable; the run page links back to its dataset **and** prompt.
- **Landing dashboard** (`/`) replacing the echo skeleton: for the selected org — prompts with
  their latest scores, recent eval runs, and open regression flags. Built on **existing read
  APIs**; net-new presentational components, container fetches.
- **Consistent page chrome.** A shared UI kit under `web/src/app/shared/`: PageHeader
  (title/subtitle/primary-action slot), Breadcrumb, and standard Loading / Empty / Error
  states, applied to every page.
- **Design-system pass.** Adopt brand primitives consistently — badges/chips for origin
  (Captured/Synthetic), scorer kind, pass/fail, target/judge model, regression severity;
  `.sb-card` grouping; button variants/sizes; `.sb-field--error` for validation. Replace raw
  emoji/plain-text status. No hardcoded hex; light + dark both verified.
- **IA reconciliation & progressive disclosure.** Resolve the three-way Prompt×Dataset
  duplication — the prompt workspace is the hub; `/datasets` and `/analytics` become clearly
  scoped secondary views (exact treatment decided in the design slice). Group the long stacked
  pages into setup / data / action, revealing secondary forms rather than always showing them.
- Retire the echo skeleton from product nav (keep the round-trip only if a smoke test needs it,
  off the primary path).

**Out (coordinated, not absorbed — cross-linked both ways):**
- **Any API/domain change.** UI-only. If the dashboard needs an aggregate the read APIs can't
  cheaply serve, it is **re-homed to a small API spec via the deferral protocol** — never added
  silently.
- **Authoring form redesign** (guided create/add-version, content editor) → stays **2.3**; this
  spec supplies the chrome/primitives 2.3 builds on.
- **Human-review UI** → stays **2.1**; this spec's shell/nav will host it.
- **New analytics content / new charts** → **1.4** owns analytics; this spec only restyles
  existing charts.

## Acceptance criteria
- [x] Topbar redesign with a global org switcher scoping prompts/datasets/analytics/dashboard; changing org updates all surfaces — `OrgContextStore` unit tests (6) + `e2e/org-context.spec.ts`
- [x] Landing dashboard at `/` shows the selected org's prompts+latest scores, recent runs, and open regressions; echo skeleton removed from nav (→ `/_skeleton`) — facade + component unit tests + `e2e/navigation.spec.ts`
- [x] Shared chrome (header/breadcrumb/loading/empty/error) applied to every page; a loading state renders before data arrives — shared-kit unit specs (16) + a loading-before-data test on the dashboard; applied to every page component
- [x] Brand primitives adopted (badges/chips, cards, button variants, field-error) replacing raw emoji/plain status; no hardcoded hex — hex-scan of `web/src` clean (0 matches); light+dark track `data-theme` by construction (brand `--sb-*` tokens only)
- [x] Runs are discoverable and a run links back to its dataset and prompt — eval-run unit test asserts both back-links; reachable from the dashboard recent-runs list
- [x] Consistent breadcrumb/back across all detail pages — shared `Breadcrumb` on prompt/dataset/eval-run detail (unit + `navigation.spec.ts`)
- [x] No prompts API/domain change and no new endpoints — UI-only; backend untouched; dashboard aggregates built from existing read APIs (deferral resolved to `built`)

## Plan (thin slices)
1. [ ] Shared UI kit under `web/src/app/shared/`: PageHeader, Breadcrumb, Loading/Empty/Error
   states, and StatusBadge + origin/scorer/severity chip helpers (map domain values ->
   `.sb-badge`/`.sb-chip`). ErrorState uses `.sb-field--error` + optional retry, replacing the
   ad-hoc `.error-box` "is the stack running?" one-liners. Jasmine spec per component. -> 52bd8a9
2. [ ] `OrgContextStore` (root signals service): `organizations`/`currentOrgId`/`currentOrg`;
   `select(id)` persists to **localStorage + `?org=` query param**; init resolves
   `?org=` -> localStorage -> first org. Topbar redesign (`Dashboard · Prompts · Analytics` +
   global org switcher). Rewire PromptList to the store; scope AnalyticsDashboard + DatasetList
   by current org (client-side join on `promptId`). Store unit tests + 1 e2e. -> e7c6e0a
3. [ ] Retire echo Home; `Dashboard` at `/` on a **dashboard facade** (bounded fan-out over
   existing read APIs -> one view-model, one loading/empty/error path). Focused widgets: org
   prompts (+ latest model/score chips), recent runs, open regressions. Echo round-trip kept
   only if `round-trip.spec.ts` needs it, off the primary path. Unit + e2e. -> c941fc5
4. [ ] Design-system pass + shared chrome across every page: pass/fail + origin + scorer +
   severity + model as badges/chips (replace raw ✅❌⚠🎉📁/plain text); PageHeader/Breadcrumb/
   Loading/Empty/Error applied everywhere; `.sb-card` grouping; button variants; `.sb-field--error`
   on validation. Run page back-links to its dataset **and** prompt (`EvalRun.datasetId`/`promptId`).
   No hardcoded hex (hex-scan); light + dark verified. Unit + inspection. -> 93de5db
5. [ ] IA reconciliation: workspace = hub; `/datasets` demoted to deep-link browse, `/analytics`
   scoped secondary; progressive disclosure on the long stacked pages (setup / data / action).
   e2e for the primary loop (prompt -> dataset -> run -> scores -> regressions). -> d53e70f

## Decisions
- 2026-07-13 — Scope set to **UX foundation** (user decision): nav/shell, global org context,
  consistent chrome, design-system pass, and a product dashboard. Coordinate with 2.3
  (authoring) and 2.1 (review) via cross-links, **not** absorption. Foundational — should land
  **before** 2.1/2.3, which build on the new shell, chrome, and primitives.
- 2026-07-13 — Organization becomes a **persistent global context** (topbar switcher) scoping
  every surface (user decision), fixing the mental-model break where org only existed on Prompts.
- 2026-07-13 — Landing becomes a **product dashboard** on existing read APIs (user decision);
  any aggregate that would need a new endpoint is re-homed via the deferral protocol, never
  added silently (keeps the "no API/domain change" guarantee intact).
- 2026-07-13 — Design constraint: **brand-tokens only** (`--sb-*`, no hex); the overhaul leans
  on already-shipped-but-unused primitives (`.sb-badge`, `.sb-chip`, `.sb-card`, button
  variants, `.sb-field--error`) rather than inventing new styles.
- 2026-07-14 — Dashboard aggregates (user decision): keep the dashboard **clean and intuitive,
  not skipped**. No org-wide "latest score / recent runs / open regressions" endpoint exists
  (each is per prompt×dataset), so the dashboard sources them via a **client-side fan-out
  encapsulated in a dashboard facade** (`forkJoin` -> one bounded view-model, one loading path)
  over existing read APIs — **deferral resolves to `built`**, no API change. If the fan-out
  proves genuinely unclean mid-build, re-home the aggregate to a small API spec via the deferral
  protocol rather than ship spaghetti (surfaced, not silent).
- 2026-07-14 — Org context (user decision): persist the selected org to **localStorage AND a
  `?org=` query param** (deep-linkable/shareable). The store owns the param write so it never
  fights router navigation.
- 2026-07-14 — IA (user decision): topbar becomes **`Dashboard · Prompts · Analytics`**.
  `/datasets` is **demoted from the topbar** (fully duplicates the workspace) — route kept for
  deep-links, reached from workspace/dashboard. `/analytics` stays as a real cross-prompt
  destination, org-scoped.

## Verification / evidence
- **Unit (Karma):** 87/87 green — `OrgContextStore` (6), shared kit (16), dashboard facade +
  component (5), rewired PromptList / AnalyticsDashboard / DatasetList / EvalRunDetail specs.
- **Production build:** `npm run build` green (AOT) after every slice.
- **Design-system:** hex-scan of `web/src` (`#[0-9a-fA-F]{3,8}`) → 0 matches; all color via `--sb-*`
  tokens, so light/dark follow `data-theme` by construction.
- **e2e (Playwright, run against the compose stack — CI `compose-smoke` / local `E2E_BASE_URL`):**
  `org-context.spec.ts` (switcher scopes prompts+analytics, persists), `navigation.spec.ts`
  (dashboard→workspace→breadcrumb loop, Datasets demoted), and the existing model-gated
  `analytics.spec.ts` covers the full run→scores→regressions loop. The five specs that drive the
  now-revealed forms were updated to open each form first. **Not executed in this dev session**
  (no stack) — pending the stack/CI run + a visual light/dark pass (user end-verification).

## Progress log
- 2026-07-13 — spec created (from a full sweep of web/: nav/IA/chrome/design-system gaps).
- 2026-07-14 `7a0fed9` — decisions recorded (dashboard fan-out → built; org = localStorage + ?org=;
  topbar Dashboard·Prompts·Analytics, /datasets demoted); set IN PROGRESS.
- 2026-07-14 `52bd8a9` — slice 1: shared UI kit (PageHeader, Breadcrumb, Loading/Empty/Error,
  StatusBadge, Chip + status mappers).
- 2026-07-14 `e7c6e0a` — slice 2: `OrgContextStore` + topbar redesign; pages rescope on org switch.
- 2026-07-14 `c941fc5` — slice 3: product `Dashboard` on a bounded facade; echo → `/_skeleton`.
- 2026-07-14 `93de5db` — slice 4: brand primitives + shared chrome app-wide; run→dataset/prompt
  back-links; no hardcoded hex.
- 2026-07-14 `d53e70f` — slice 5: progressive disclosure on the workspace pages; IA reconciliation;
  `navigation.spec.ts`.
