---
id: 0.1
title: Walking Skeleton
links: [1.1, 1.2, 1.3, 3.2]
---

## Problem
Establish the minimal end-to-end skeleton so every subsequent spec builds on a working
system — Angular, the .NET API, the Python eval-runner, and PostgreSQL all wired together
and reachable, even though they do nothing useful yet. One trivial prompt runs through the
whole path to prove the seams.

## Value
As a developer I want to run LitmusAI locally and reach every layer end to end so
that subsequent specs add behavior to a proven skeleton rather than building in isolation.

## Scope
**In:**
- .NET solution with the four DDD projects (`Domain`, `Application`, `Infrastructure`,
  `Api`) plus per-layer test projects; DI wiring in `Api`.
- One trivial vertical slice: a hardcoded prompt string → API endpoint → eval-runner echo
  → response, persisted as a single `EvalRun` row in Postgres.
- Python eval-runner FastAPI app with one `/health` and one echo/score endpoint.
- Angular app that calls the API and displays the round-trip result, styled with the
  shared **brand-tokens** package (`github:liminalarc/brand-tokens#v0.4.0`): import
  `brand-tokens/css` (+ optionally `/components`) globally, theme via `data-theme`, and
  reference `--sb-*` custom properties only — no hardcoded hex.
- EF Core + Npgsql wired with one migration; Postgres reachable locally.
- **Full containerized runtime**: a `Dockerfile` per service (`api`, `eval-runner`, `web`)
  and a `docker-compose.yml` orchestrating all four services (`api`, `eval-runner`, `web`,
  `db`/Postgres) on a **shared Docker bridge network**, so services address each other by
  name (`api` → `eval-runner:8000`, `api` → `db:5432`) rather than `localhost`.
  - Healthchecks on every service; `depends_on` with `condition: service_healthy` so start
    order is correct (db → eval-runner → api → web).
  - A named volume for Postgres data; migrations applied on API startup (or an init step).
  - `.env` / compose env wiring for the connection string, `EvalRunner__BaseUrl`, and
    `ANTHROPIC_API_KEY` (not needed for the echo slice, but the wiring exists).
  - `docker compose up` brings the whole stack healthy from a clean machine.
- Basic CI: build + lint + the (minimal) tests for all three layers; CI also builds the
  images and runs a compose smoke test (stack comes up healthy).

**Out:** real prompt registry (1.1), real datasets/fixtures (1.2), real scorers (1.3),
analytics (1.4). No LLM call yet — the eval-runner endpoint just echoes. Production
orchestration (k8s/hosting) is out — compose is the local + CI runtime only.

## Acceptance criteria
- [x] `dotnet build` succeeds; `dotnet run --project Api` serves the API — proven by hitting the health endpoint
- [x] `uvicorn` serves the eval-runner; `/health` returns 200 — proven by curl
- [x] `npm start` serves the Angular app and displays a successful end-to-end round trip — proven in the browser
- [x] `docker compose up` from a clean checkout brings **all four services** (`api`, `eval-runner`, `web`, `db`) up **healthy** on a shared network — proven by `docker compose ps` all-healthy
- [x] Services reach each other by **service name** over the Docker network (api→eval-runner, api→db), not `localhost` — proven by the end-to-end round trip succeeding inside compose
- [x] Postgres data survives a container restart via a named volume — proven by restart + data-present check
- [x] All layers communicate end-to-end (Angular → API → eval-runner → Postgres and back) — proven by one manual run + one e2e test, run against the composed stack
- [x] Local setup documented in `README.md` (both per-process **and** `docker compose up`) and followed clean — proven by a from-scratch run
- [x] Basic CI passes (lint + minimal tests for all three layers) **and** a compose smoke test (stack up healthy)

## Plan (thin slices)
1. [x] .NET solution + four projects + test projects; health endpoint. -> `0a81fe2`
2. [x] Domain `EvalRun` + Application ports + `CreateEvalRun` use case. -> `b81200c`
3. [x] EF Core + Npgsql + `EvalRun` mapping + initial migration. -> `80968ea`
4. [x] eval-runner FastAPI app: `/health` + echo endpoint; pytest for both. -> `3a33363`
5. [x] Api → eval-runner HTTP client (`IEvaluationRunner` echo impl); persist one `EvalRun`. -> `14b8e8d`
6. [x] Angular app: call API, show round-trip result, styled via brand-tokens
       (`brand-tokens/css` + `data-theme`, `--sb-*` only); Playwright e2e. -> `6eb9f42`
7. [x] `Dockerfile` per service (api, eval-runner, web). -> `a43a609`
8. [x] `docker compose up` — all four services on a shared network with healthchecks,
       ordered `depends_on`, Postgres named volume, env wiring; services address by name;
       end-to-end round trip works inside compose. -> `e30ca59`
9. [x] CI pipeline: build + lint + tests across all three layers **+ compose smoke test**;
       register these jobs as **required status checks** on the `main` branch protection. -> `4e8b1fe`, `c1f4a8f`

(The .NET work split into slices 1–3 and CI into 9 for cleaner test-first commits;
the spec's original 8-slice intent is preserved.)

## Decisions
- 2026-07-11 — Skeleton echoes rather than calling Claude, to prove the seams without
  coupling the first slice to model behavior or an API key.
- 2026-07-11 — The skeleton stands up the **full containerized runtime** (docker-compose,
  all four services, shared network, healthchecks, named volume), not just per-process
  dev servers. Services address each other by name over the Docker network so the
  networking topology is proven from day one and every later spec inherits a working stack.
  Compose is the local + CI runtime; production orchestration is out of scope.

- 2026-07-11 — `main` branch protection is live (PR-required, 0 approvals, linear history,
  no force-push/deletion) but with **no required status checks** yet. Slice 8 wires the CI
  jobs in as required checks; until then protection gates merges but nothing gates on green.

- 2026-07-11 — Adopt the shared **brand-tokens** repo (`liminalarc/brand-tokens`, latest
  `v0.4.0`) as the web app's styling source of truth from the skeleton on, so Prompt
  Evaluator is visually consistent with the other apps and re-themes centrally. Consumed as
  a git dependency (no registry); components reference `--sb-*` custom properties only.
  Dependabot/Renovate can later auto-PR token bumps.

- 2026-07-12 — Pinned to **Angular 20** (not the latest 22): the local/CI Node is 20.19
  and Angular 22 requires Node 22+. Revisit when the toolchain moves to Node 22.
- 2026-07-12 — Pinned the whole **EF Core stack incl. `Microsoft.EntityFrameworkCore.Relational`
  to 10.0.9** in Api + Infrastructure. Npgsql 10.0.3 floats Relational to 10.0.4 while Core
  resolved to 10.0.9, so the published (container) API couldn't load Relational 10.0.9 at
  runtime. Explicit alignment ships the matching provider assemblies.
- 2026-07-12 — The API applies EF migrations **on startup** (guarded on a configured
  connection string) rather than a separate init container, so a clean `compose up` is
  self-migrating and the bare `/health` test needs no database.
- 2026-07-12 — The Angular app calls **relative `/api/...`**; the ng-serve proxy (dev) and
  nginx (compose) route it. No environment-specific base URL in the client.

## Verification / evidence
- **API `/health`** — `WebApplicationFactory<Program>` integration test (`Api.Tests`), green.
- **eval-runner `/health` + `/echo`** — pytest (`tests/test_main.py`), 3 tests green; ruff clean.
- **Persistence + migration** — `EvalRunRepositoryTests` adds then reads an `EvalRun` through
  the migrated schema on a real Postgres (Testcontainers), green.
- **Endpoints end-to-end (in-proc)** — `EvalRunsEndpointTests`: POST creates → persists →
  GET retrieves, plus 404, against Testcontainers Postgres with a faked runner.
- **Compose all-healthy** — `docker compose up --wait` → `docker compose ps` shows db,
  eval-runner, api, web all `healthy`.
- **Name-based addressing + round trip in compose** — POST `localhost:4200/api/eval-runs`
  (nginx → `api:8080` → `eval-runner:8000` → `db:5432`) returns the echoed output; the
  Playwright e2e (`round-trip.spec.ts`) drives the same flow through the browser.
- **Named-volume durability** — created a row, `docker compose down` then `up`, GET by id
  returned the same row (migrate-on-startup idempotent).
- **CI** — GitHub Actions run [29176454896](https://github.com/liminalarc/prompt-evaluator/actions/runs/29176454896):
  backend / eval-runner / web / compose-smoke all green; registered as required status
  checks on `main`.

## Progress log
- 2026-07-11 — spec created
- 2026-07-12 `0a81fe2` — .NET DDD solution scaffold + `/health`
- 2026-07-12 `b81200c` — `EvalRun` aggregate + Application ports + `CreateEvalRun`
- 2026-07-12 `80968ea` — EF Core + Npgsql + initial migration (Testcontainers-verified)
- 2026-07-12 `3a33363` — eval-runner FastAPI `/health` + `/echo`
- 2026-07-12 `14b8e8d` — HTTP runner adapter + `/api/eval-runs` endpoints + DI
- 2026-07-12 `6eb9f42` — Angular SPA round-trip UI on brand-tokens
- 2026-07-12 `a43a609` — Dockerfile per service
- 2026-07-12 `e30ca59` — docker-compose full stack (shared network, healthchecks, volume)
- 2026-07-12 `4e8b1fe`, `c1f4a8f` — GitHub Actions CI + compose smoke; pytest pythonpath fix
- 2026-07-12 — CI green; `backend`/`eval-runner`/`web`/`compose-smoke` set as required checks on `main`
- 2026-07-12 `dae35a2` — spec close-out (DONE + archive); SHAs above reconciled to post-rebase `main`
- 2026-07-12 `c2479ea` — host ports moved to a dedicated 4240–4243 block (web 4240, api 4241,
  eval-runner 4242, db 4243) to avoid local collisions
- 2026-07-12 `f68c645` — follow-up: `GET /version` on the API (aggregating its own
  version+commit, a live eval-runner probe, and Postgres `SELECT version()`) and a standalone
  `/version` on eval-runner; commit SHA baked at image build via the `GIT_COMMIT` arg
- 2026-07-12 — Deferral reconciliation (per *No silent deferrals*): the deferred "production
  orchestration (hosting/k8s)" was re-homed to **spec 3.2** (Production Deployment) by user
  decision. `/flow-ship` stops at a tagged build until 3.2 lands.
