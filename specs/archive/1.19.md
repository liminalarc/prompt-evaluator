---
id: 1.19
title: Catalog: add current Anthropic models (Sonnet 4.6, Opus 4.7, Opus 4.6)
links: [1.13, 5.1]
---

## Problem
The seeded Model Catalog (1.13) carries Opus 4.8 / Sonnet 5 / Haiku 4.5 / GPT-4o(-mini) but **not**
Sonnet 4.6, Opus 4.7, or Opus 4.6 — all current, active Anthropic models. Dogfooding ([[5.1]]) hit this:
Cortex Golf runs several prompts on **Sonnet 4.6**, which isn't selectable as a target model, so a
faithful baseline can't be run. **Shipped** = those three models seed into the catalog and are
selectable for subject/judge/generator, same as the others.

## Value
As an operator I want to pick the model my app actually runs so the eval baseline transfers to production.

## Scope
**In:** seed `claude-sonnet-4-6` ($3/$15), `claude-opus-4-7` ($5/$25), `claude-opus-4-6` ($5/$25) —
Anthropic, all roles (subject/judge/generator), active. A new EF data-migration (never edit the applied
1.13 seed). Same adaptive-thinking family as the working Opus 4.8 / Sonnet 5, so no eval-runner change.
**Out:** Fable 5 (held — thinking always-on, 30-day-retention requirement, refusal path, higher cost;
revisit when the judge/subject path is confirmed for it). Live model discovery → [[1.14]]. Model ids
verified via the `claude-api` skill.

## Acceptance criteria
- [x] `claude-sonnet-4-6`, `claude-opus-4-7`, `claude-opus-4-6` are seeded (Anthropic, all roles, active, priced) — test
- [x] `/api/models` returns all three alongside the existing five — test
- [x] Existing seed rows unchanged (new migration, applied 1.13 migration untouched) — verified

## Plan (thin slices)
1. [x] Test-first: assert the three new models in `ModelsEndpointTests` -> `<sha>`
2. [x] EF data-migration inserting the three rows (+ Down delete) -> `<sha>`

## Decisions
- 2026-07-18 — Models chosen (user): Sonnet 4.6 + Opus 4.7 + Opus 4.6; Fable 5 held (quirks/cost).
- 2026-07-18 — Data-only migration (InsertData/DeleteData), no snapshot/schema change; well-known Guids
  for idempotency, matching the 1.13 seed pattern.

## Verification / evidence
Backend xUnit green; `/api/models` on dev lists the three after deploy; catalog droplist offers them.

## Progress log
- 2026-07-18 — created from [[5.1]] fidelity need (round-debrief runs on Sonnet 4.6).
- 2026-07-18 `09db857` — seeded the three models (data-only migration) + test; `ModelsEndpointTests` green
  (9/9, migration applies against Testcontainers Postgres). Ships to dev with the next release.
